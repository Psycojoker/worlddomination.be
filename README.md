## Installation

Create a virtualenv (python 3.7 for now, lol)

    pip install -r ./requirements.txt

Re-add plugins:

    lektor plugins reinstall

Then you should be able to run:

    lektor build
